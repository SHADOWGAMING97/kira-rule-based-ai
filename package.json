{
  "name": "kira-brain1",
  "version": "0.1.0",
  "description": "Kira — Lucky's personal AI, thinking-brain-only Capacitor Android app",
  "type": "module",
  "scripts": {
    "sync": "npx cap sync android",
    "verify": "node verify-build.js",
    "build:android": "npx cap sync android && node verify-build.js && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/preferences": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "^5.4.5"
  }
}
